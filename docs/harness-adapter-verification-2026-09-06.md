# Harness adapter verification, 2026-09-06

## Scope and recurring failure causes

The existing registry already contains converters for Codex, Claude Code, Kimi Code, OpenCode and DeepSeek Harness; Xiaowan consumes the configured Provider directly. Keep these owners and the canonical ACP lifecycle. More registry names alone do not improve compatibility.

Concrete defects found:

- OpenCode always selected the Chat Completions SDK, including Responses/Anthropic profiles. Select `@ai-sdk/openai-compatible`, `@ai-sdk/openai`, or `@ai-sdk/anthropic` from configured protocol/wire.
- OpenCode raw namespaced model IDs could lose the first segment. Remove only the managed `omnibot/` prefix.
- Existing managed OpenCode model entries survived a refreshed nonempty catalog. Remove obsolete managed entries, preserving remaining model settings and unrelated custom providers. An absent catalog does not erase configuration.
- Kimi's stale OpenAI Responses setting overrode an explicitly selected Anthropic protocol. Explicit protocol now owns both environment and config-file selection.
- Codex silently mapped an Anthropic profile to Responses. Reject that incompatible configuration before launch; do not synthesize a converter or change the user's endpoint.
- Claude Code's phone error is the existing protocol guard. The selected provider is configured as OpenAI; this does not prove the remote service lacks a separate Anthropic interface. Preserve the guard and show an actionable localized explanation.

## Primary references

- https://opencode.ai/docs/providers/ — custom Provider SDK selection distinguishes Chat Completions and Responses.
- https://ai-sdk.dev/providers/ai-sdk-providers/anthropic — Anthropic SDK, API root, headers.
- https://code.claude.com/docs/en/llm-gateway — Claude Code gateway contract.

## Reproducible wire check

Install `ai`, `@ai-sdk/openai`, `@ai-sdk/openai-compatible`, and `@ai-sdk/anthropic` outside the repository, then:

```sh
node scripts/verify-harness-provider-wires.mjs /absolute/sdk-directory
```

All three cases passed: exact endpoint, unmodified namespaced model, matching payload shape, one attempt. The test uses the real SDK serializers and an intercepted intentional 401 response, with no real credentials or network calls. It is not full Harness or real-provider inference acceptance.

Native regression coverage is in `AgentConfigAdaptersTest`: official mapping, credentials/headers, model catalogs, reasoning metadata, known Claude endpoint normalization, unknown endpoint rejection, Kimi configuration transitions, OpenCode protocol switching and persisted catalog replacement. Full real-account inference across all six Harnesses remains a separate acceptance requirement.

## Local result

`AgentConfigAdaptersTest`: 45 tests, 0 failures, 0 errors. Includes all six new regression cases. Combined test/APK build succeeded in 3m25s. Isolated APK SHA256: `557ad099fdc5d9590dc01b82719a6dcaaca522632f2c068f0a0934c1f87fb023`.

Phone V2502A installation completed with `adb install -r`: Success. Post-install UI inspection found another application foreground; no model setting or credentials were changed. This run does not claim completed phone interaction acceptance or real inference across all Harnesses.

## Full catalog follow-up

153 native tests passed across AgentConfigAdapters, AgentAdapterCatalog, AgentRuntimeManagerConfig, AgentRuntimeProtocolPayload, XiaowanProviderCache, and AgentWebRuntime. The new catalog test uses all six shipped profiles; unknown declared adapter IDs now fail explicitly. Custom ACP profiles with no converter remain supported. Obsolete DSH test assertions were updated to the shipped official ACP profile/patch installation contract.

Real installed macOS CLIs consumed fixtures generated by the production Kotlin configuration adapters. All 12 accepted combinations completed ACP initialize, session/new, prompt, and official PromptResponse against a local deterministic success server. The nine combinations advertising thought configuration also accepted session/set_config_option; OpenCode correctly exposed no levels for this unknown test model. Results and exact CLI versions are under `verification/harness-adapters-20260906/`. These are actual CLI/wire/config tests, not real-account inference or Android execution acceptance.

This run found and fixed duplicate /v1 paths in Claude Code and DSH Anthropic requests. Shared service-root normalization now removes the SDK-owned suffix without guessing a service-specific route. OpenCode now receives declared model reasoning/tool/temperature/attachment flags and token limits; no reasoning levels are invented. It remains responsible for its official variants.

Reproduce with temporary installed CLI packages, then:

```sh
node scripts/verify-installed-harness-adapters.mjs /absolute/cli-directory app/build/reports/harness-adapters
```

Final APK build succeeded. SHA256: `957d908f8ee6bd21224bb423d0bf3d9efb1b9d784889a48f98d3da7779f45d61`. Phone installation was blocked because serial 10AG2P03S5004ZD disconnected; no data was cleared.

Outstanding host/account acceptance: no end-to-end real credentials across every service, no process-restart settings test across every external Harness in this run, and the concurrent Android task reported DSH's workspace-write sandbox backend unavailable under PRoot. Do not mask that by defaulting to danger-full-access. An API/model/config adapter passing does not imply every shell sandbox/tool capability works on every host.
