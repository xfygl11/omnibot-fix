# 0.6.2.3 统一候选版本

已将累计集成分支及其剩余源码、测试、场景和说明文档固定到
`codex/release-0.6.2.3`，目录为
`/Users/wuzewen/Projects/Omni/OpenOmniBot-release-0.6.2.3`。
版本设置为 `0.6.2.3 / versionCode 15`。

来源：`codex/integrate-local-fixes@6c7b41b03` 加 2026-09-09 工作树快照。
本次纳入 84 个已有文件改动和 94 个新增源码/测试/说明文件；随后增加本文。
原始日志、截图等 1431 个本地证据文件保留在原集成目录，没有批量复制进提交。
一些说明里的 artifacts 相对链接需要回到原集成目录查看。

## 相对 0.6.2.2 的修改

统计基线是发布提交 `4ccc7bbbc`，不是上一个临时补丁包。
下表按用户行为归为 **14 类修改**，不是声称修复了 14 个已验收 bug。

| 编号 | 修改类别 | 具体内容 |
| --- | --- | --- |
| 1 | 请求前上下文预算 | 首次全量估算、后续 usage 加新消息、工具定义预算；超预算先走已有压缩器 |
| 2 | 摘要与历史恢复 | 大结果完整外存、共享预算、历史逐页恢复、可靠 checkpoint、保留最新工具控制元数据 |
| 3 | Provider 错误与超限 | 有限超限恢复、已开始输出不重放；鉴权/限流/服务端/流内错误准确结束和展示 |
| 4 | 文件与图片工具 | 小块 maxChars、Unicode/单行文件分页、内容检索与目录遍历、原图读取及外存正文取回 |
| 5 | ACP 会话生命周期 | 初始化/配置/关闭/删除/取消边界，worker 先登记后执行，关闭时拒绝新执行，历史与会话绑定 |
| 6 | MCP 与权限 | 超时、SSE 取消、拒绝与停止恢复，Shizuku 服务绑定及相应回归 |
| 7 | 终端执行与停止 | stdin 与退出码保留、持久终端恢复、子进程停止和会话清理，不靠结束 App 终止任务 |
| 8 | Codex 工具结果与历史 | 失败详情/退出码、旧历史兼容、确认卡已有结果；标题请求沿用配置模型，安装修订标识更新 |
| 9 | 聊天切换与输入 | Harness 切换发送边界、初始化/取消反馈、草稿与附件保留、会话刷新 |
| 10 | 聊天展示 | 完成耗时持久化、紧凑统计、无 usage 零进度圈、历史列表懒加载 |
| 11 | 权限弹层返回 | 系统返回关闭弹层并保留聊天，不直接退出 App；保持配置值 |
| 12 | 文件预览排版 | HTML/Word 等资源卡按完整高度占位，后续标题/正文/表格不重叠，支持加载变高和重建 |
| 13 | 定时任务与配置 | 定时创建/失败结果/重启持久化，Provider 配置与模型缓存一致性 |
| 14 | 可执行回归与证据 | Agent/ACP、文件、MCP、终端、上下文、UI/WebChat 统一入口；精确终态和重启场景 |

补齐忽略文件后的合并统计共 **405 个文件**：产品代码及资源 87、测试及验证脚本 190、
构建/依赖 3、说明及来源记录 125。说明和测试占多数，不能把 405 当作功能数。
完整复算入口：`git diff --stat 4ccc7bbbc HEAD`。

## 去重与未纳入项

- 主目录本轮的 PiContextTokenEstimate 最小补丁已被集成分支的 AgentContextBudget
  覆盖，没有叠加第二套预算/摘要实现；原目录保留。
- 预览修复由 e0bf4201f 经 f409364ee 纳入，不重复合并。
- 插件市场、Project runtime/market、旧 OmniFlow 分支仍是外部待审查候选，
  没有恢复已移除的独立 App/WebView 或另一套 Agent 生命周期。
- Codex/DSH 沙箱仍有已复现失败；错误显示改好不代表沙箱已修复。
- 本次固定候选没有修改签名、凭据、GitHub 工作流或运行中设备数据。

## 验证状态

排版导入后的 6 项测试已经通过。独立排版 0.6.2.2 包在模拟器验证过
原消息、滚动、打开返回和重启恢复；该安装不能替代本候选验收。

本次已在原集成目录运行 `scripts/test-agent-runtime.sh --offline`，避免重复
生成同一套编译缓存。开始后核对了候选与该目录 app/src、baselib/src、ui/lib、
ui/test、scripts、ReTerminal/core/main/src 的文件字节，无差异；版本元数据除外。
回归结果须记录退出状态和实际范围，不能把历史通过记录当成本次结果。

本候选尚未生成/安装最终 0.6.2.3 APK，没有物理手机验收。**待真机验证**。


## 本次固定候选的离线回归结果

2026-09-09，维护入口退出码 0，日志 `/tmp/oob-0623-regression.log`：
Node 88、Python 55、App Agent/ACP JVM 710（77 类，失败/错误/跳过均为 0）、
Flutter 729、WebChat 12 项通过，WebChat typecheck/build 通过。
Shizuku 检查 Gradle 成功；缓存复用不另计为新增执行测试。
真实 Provider 明确 SKIPPED，外部 Harness 输出验收明确 INCOMPLETE；未进行浏览器验收。
完成后再次逐字节核对测试目录与候选相关源码，无差异。
核对文件数：1511；路径及内容集合 SHA-256：`f41523f0b5eda8944eac3fe7a925e20740f158657933de47368c66dc49656064`。


## 独立构建补充（2026-09-10）

独立候选首次构建失败：closeMcpProcess 未解析。根因是原仓库的本地
.git/info/exclude 中 runtime/ 等规则屏蔽了新增源码、测试和断言脚本。
此前只比较 Git 可见文件的字节，未覆盖被忽略的源文件，故不能据此证明
候选完整。已补入原目录已有的26个源码/测试/脚本（含McpProcessCleanup），
没有另写一套关闭逻辑，也没有改用户本地忽略配置。
新版在独立目录实际构建和执行 runtime 测试；首轮失败证据保留在
/tmp/oob-0623-build.log，补齐后的执行日志为 /tmp/oob-0623-build-2.log。
[人工验收步骤](0.6.2.3-manual-check.md) 列明操作、预期和未覆盖边界。

补齐后独立构建与 runtime 测试成功（1m5s），并已覆盖安装到 emulator-5554，
版本 0.6.2.3 / 15。先前“尚未生成/安装”是补齐前的历史状态，以本段为准。
完整维护入口在候选目录重跑，结果见后续记录；人工验收等待用户执行。


### 候选目录独立回归完成

在 `OpenOmniBot-release-0.6.2.3` 直接执行 `scripts/test-agent-runtime.sh --offline`，退出码 0，Selected local checks passed。日志：`/tmp/oob-0623-candidate-regression.log`。真实 Provider smoke 未运行，外部 Harness output acceptance 未完成；模拟器人工用例待用户执行，待真机验证。此项仅补充验证记录，安装 APK 的产品代码未再改变。
