name: ui
description: "A new Flutter module project."

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
#
# This version is used _only_ for the Runner app, which is used if you just do
# a `flutter run`. It has no impact on any other native host app that you embed
# your Flutter project into.
version: 1.0.0+1

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.2"

dependencies:
  crypto: 3.0.7
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.5.5
  intl: ^0.20.3
  loading_animation_widget: ^1.3.0
  flutter_markdown_plus: ^1.0.12
  carousel_slider: ^5.1.2
  permission_handler: ^13.0.1
  url_launcher: ^6.3.2
  flutter_slidable: ^4.0.3
  flutter_switch: ^0.3.2
  simple_gradient_text: ^1.4.0
  dotted_border: ^3.1.0
  gradient_borders: ^1.0.4


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9
  http: ^1.6.0
  flutter_riverpod: ^3.4.2
  riverpod_annotation: ^4.0.6
  webview_flutter: ^4.14.1
  webview_flutter_android: ^4.10.13
  fluttertoast: ^10.0.0
  go_router: ^18.0.0
  flutter_svg: ^2.3.0
  gal: ^2.3.3
  image_picker: ^1.2.3
  cached_network_image: ^4.0.0
  flutter_cache_manager: ^3.4.2
  dio: ^5.11.0
  path_provider: ^2.1.6
  file_picker: ^12.2.0
  android_file_picker: ^1.1.0
  just_audio: ^0.10.6
  archive: ^4.2.0
  markdown: ^7.3.1
  flutter_math_fork: ^0.7.4
  video_player: ^2.14.0
  xml: ^7.0.1
  mobile_scanner: ^7.4.0
  lucide_icons_flutter: ^3.1.17
  qr_flutter: ^4.1.0

dev_dependencies:
  webview_flutter_platform_interface: ^2.15.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.16.0
  riverpod_generator: ^4.0.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  generate: true
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add Flutter specific assets to your application, add an assets section,
  # like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/avatar/
    - assets/home/
    - assets/home/chat/
    - assets/home/date_header_icons/
    # Legacy onboarding art and scene backgrounds are intentionally excluded.
    # Only the permission-guide assets still reachable from the current app
    # are packaged.
    - assets/welcome/auto_start_guide_1.png
    - assets/welcome/auto_start_guide_2.png
    - assets/welcome/auto_start_guide_3.png
    - assets/welcome/distro_alpine.svg
    - assets/welcome/distro_ubuntu.svg
    - assets/welcome/next_page.svg
    - assets/welcome/permission_authorized.svg
    - assets/welcome/permission_autostart.svg
    - assets/welcome/permission_battery.svg
    - assets/welcome/permission_go.svg
    - assets/welcome/permission_icon.png
    - assets/welcome/permission_installed_apps.svg
    - assets/welcome/permission_overlay.svg
    - assets/common/
    - assets/memory/
    - assets/my/
    - assets/app_icons/
    - assets/webview/
    - assets/feedback/
    - assets/loading/
    - assets/chatbot/
    - assets/provider_icons/
    - assets/agents/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add Flutter specific custom fonts to your application, add a fonts
  # section here, in this "flutter" section. Each entry in this list should
  # have a "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package


  # This section identifies your Flutter project as a module meant for
  # embedding in a native host app.  These identifiers should _not_ ordinarily
  # be changed after generation - they are used to ensure that the tooling can
  # maintain consistency when adding or modifying assets and plugins.
  # They also do not have any bearing on your native host application's
  # identifiers, which may be completely independent or the same as these.
  module:
    androidX: true
    androidPackage: com.example.ui
    iosBundleIdentifier: com.example.flutterModule
